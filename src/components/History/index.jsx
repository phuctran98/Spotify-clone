import React from 'react';
import PropTypes from 'prop-types';

History.propTypes = {
    
};

function History(props) {
    return (
        <div>
            
        </div>
    );
}

export default History;