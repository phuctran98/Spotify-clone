import React from 'react';
import "./Hero.css"
import PropTypes from 'prop-types';

Hero.propTypes = {
    
};

function Hero(props) {
    return (
        <div className="hero">
            <h1>Locking music</h1>
        </div>
    );
}

export default Hero;